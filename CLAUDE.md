# CLAUDE.md

**See [AGENTS.md](AGENTS.md).**

All agent-facing instructions for this repository live in a single tool-agnostic `AGENTS.md`.
This file exists only because Claude Code loads `CLAUDE.md` by default — it is a pointer, not a
second source of truth. Don't add instructions here; add them to `AGENTS.md` so every agent,
whatever harness it runs under, sees the same rules.

Porting status, module order, and open questions: [ROADMAP.md](ROADMAP.md).
